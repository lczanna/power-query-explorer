[project]
name = "power-query-explorer"
version = "0.1.0"
description = "Browser-based Power Query M extractor and visualizer with helper scripts for fixtures and tests"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1.5",
]
